<%- include('../partials/head.ejs') %>

    <body>
        <%- include('../partials/navbar.ejs') %>
            <main class="pt-16">
                <!-- Hero Section - Dark Professional Theme -->
                <section class="relative min-h-[50vh] flex items-center bg-gradient-to-b from-slate-900 via-slate-800 to-slate-900 overflow-hidden" id="portfolioHero">
                    
                    <!-- Animated Background Elements -->
                    <div class="absolute inset-0">
                        <!-- Gradient Blobs -->
                        <div class="absolute bg-blue-600 rounded-full top-1/4 left-1/4 w-96 h-96 mix-blend-multiply filter blur-3xl opacity-20 animate-blob"></div>
                        <div class="absolute bg-cyan-600 rounded-full top-1/3 right-1/4 w-96 h-96 mix-blend-multiply filter blur-3xl opacity-20 animate-blob animation-delay-2000"></div>
                        <div class="absolute bg-purple-600 rounded-full bottom-1/4 left-1/2 w-96 h-96 mix-blend-multiply filter blur-3xl opacity-20 animate-blob animation-delay-4000"></div>
                    </div>

                    <!-- Grid Pattern Overlay -->
                    <div class="absolute inset-0 bg-grid-pattern opacity-5"></div>

                    <!-- Floating Geometric Shapes -->
                    <div class="absolute inset-0 pointer-events-none overflow-hidden">
                        <div class="absolute top-20 left-1/4 w-16 h-16 border-2 border-blue-400/20 rounded-lg rotate-12 animate-float"></div>
                        <div class="absolute top-40 right-1/3 w-12 h-12 border-2 border-cyan-400/20 rounded-full animate-float" style="animation-delay: 0.5s;"></div>
                        <div class="absolute bottom-32 left-1/3 w-20 h-20 border-2 border-purple-400/20 rounded-lg -rotate-12 animate-float" style="animation-delay: 1s;"></div>
                        <div class="absolute bottom-20 right-1/4 w-14 h-14 border-2 border-pink-400/20 rounded-full animate-float" style="animation-delay: 1.5s;"></div>
                    </div>
                    
                    <div class="relative z-10 max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8 py-20 md:py-24">
                        <div class="mb-12 md:mb-16 lg:mb-20 text-center">
                            <!-- Badge -->
                            <div class="inline-flex items-center gap-2 px-4 py-2 mb-6 border rounded-full bg-gradient-to-r from-blue-500/20 to-cyan-500/20 border-blue-500/30 backdrop-blur-sm animate-fade-in">
                                <span class="relative flex h-2 w-2">
                                    <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-cyan-400 opacity-75"></span>
                                    <span class="relative inline-flex rounded-full h-2 w-2 bg-cyan-400"></span>
                                </span>
                                <svg class="w-4 h-4 md:w-5 md:h-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10">
                                    </path>
                                </svg>
                                <span class="text-xs font-semibold text-gray-300 md:text-sm tracking-wide">OUR PORTFOLIO</span>
                            </div>

                            <!-- Main Heading -->
                            <h1 class="mb-6 text-4xl md:text-5xl lg:text-6xl font-extrabold leading-tight animate-fade-in-up text-white">
                                <span class="block mb-2">Showcasing Our</span>
                                <span class="block text-transparent bg-clip-text bg-gradient-to-r from-blue-400 via-cyan-400 to-blue-400">
                                    Digital Excellence
                                </span>
                            </h1>
                            
                            <!-- Description -->
                            <p class="max-w-3xl mx-auto text-gray-300 text-sm md:text-base lg:text-lg leading-relaxed animate-fade-in-up" style="animation-delay: 0.2s;">
                                Explore our diverse portfolio of successful projects spanning 
                                <span class="font-semibold text-blue-400">web development</span>, 
                                <span class="font-semibold text-purple-400">graphic design</span>, and 
                                <span class="font-semibold text-cyan-400">digital marketing</span> solutions that have helped businesses thrive.
                            </p>
                        </div>

                </section>

                <!-- CTA Section -->
                <section
                    class="flex items-center justify-start md:min-h-screen p-8 sm:p-16 py-10 bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900">
                    <div class="max-w-4xl mx-auto">
                        <h1 class="mb-4 text-5xl font-extrabold leading-tight text-white md:text-8xl">
                            Got a Project in<br>
                            Mind?
                            <span class="inline-flex items-center avatar-stack">
                                <img class="avatar"
                                    src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?q=80&w=1964&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D"
                                    alt="Avatar 1">
                                <img class="avatar"
                                    src="https://images.unsplash.com/photo-1500648767791-00dcc994a43e?q=80&w=1974&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D"
                                    alt="Avatar 2">
                                <img class="avatar"
                                    src="https://images.unsplash.com/photo-1506794778202-cad84cf45f1d?q=80&w=1974&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D"
                                    alt="Avatar 3">
                            </span>
                        </h1>
                        <h2
                            class="relative inline-block pb-2 text-5xl font-extrabold leading-tight text-transparent md:text-8xl bg-clip-text bg-gradient-to-r from-blue-400 to-cyan-400">
                            Let's Talk!
                            <span class="absolute bottom-0 left-0 w-2/3 h-1 bg-accent-pink"></span>
                        </h2>
                
                        <div class="flex items-center mt-2 space-x-3 text-lg md:mt-12">
                            <i class="text-white fa-regular fa-envelope"></i>
                            <a href="mailto:hello@designstudiouiux.com"
                                class="text-white hover:text-accent-pink hover:underline">contact@itbizone.com</a>
                        </div>
                    </div>
                </section>

                <!-- JavaScript for Portfolio Filtering -->
                <script>
                    function filterProjects(category) {
                        const projects = document.querySelectorAll('.project-item');
                        const buttons = document.querySelectorAll('.filter-btn');

                        // Remove active class from all buttons
                        buttons.forEach(btn => {
                            btn.classList.remove('active', 'bg-main', 'text-white');
                            btn.classList.add('bg-white', 'text-gray-700');
                        });

                        // Add active class to clicked button
                        event.target.classList.add('active', 'bg-main', 'text-white');
                        event.target.classList.remove('bg-white', 'text-gray-700');

                        // Show/hide projects based on category
                        projects.forEach(project => {
                            if (category === 'all' || project.classList.contains(category)) {
                                project.style.display = 'block';
                                project.style.opacity = '1';
                            } else {
                                project.style.display = 'none';
                                project.style.opacity = '0';
                            }
                        });
                    }

                    // Initialize with all projects visible
                    document.addEventListener('DOMContentLoaded', function () {
                        filterProjects('all');
                        
                        // Animated Counter for Stats
                        const counters = document.querySelectorAll('.counter');
                        const speed = 200; // Animation speed
                        
                        const animateCounters = () => {
                            counters.forEach(counter => {
                                const target = +counter.getAttribute('data-target');
                                const count = +counter.innerText;
                                const increment = target / speed;
                                
                                if (count < target) {
                                    counter.innerText = Math.ceil(count + increment);
                                    setTimeout(() => animateCounters(), 1);
                                } else {
                                    counter.innerText = target + (counter.parentElement.querySelector('span') ? '' : '+');
                                }
                            });
                        };
                        
                        // Intersection Observer for counter animation on scroll
                        const statsSection = document.querySelector('#portfolioHero');
                        let counterAnimated = false;
                        
                        const observerOptions = {
                            threshold: 0.5,
                            rootMargin: '0px'
                        };
                        
                        const observer = new IntersectionObserver((entries) => {
                            entries.forEach(entry => {
                                if (entry.isIntersecting && !counterAnimated) {
                                    animateCounters();
                                    counterAnimated = true;
                                }
                            });
                        }, observerOptions);
                        
                        if (statsSection) {
                            observer.observe(statsSection);
                        }
                    });
                </script>

            </main>
            <%- include('../partials/footer.ejs') %>

    </body>

    </html>
